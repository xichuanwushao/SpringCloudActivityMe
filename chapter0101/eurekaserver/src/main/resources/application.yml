server:
  port: 8761
spring:
  application:
    name: chapter_server
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/chapter_server/eureka/
